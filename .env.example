# Pin a release tag before deploying to a long-lived server.
TELEGRAM_GATEWAY_IMAGE=ghcr.io/klhq/telegram-gateway:latest
TELEGRAM_GATEWAY_PORT=8000

# Keep these values in your local .env file. Do not commit that file.
TELEGRAM_BOT_TOKEN=
GATEWAY_API_KEY=
WEBHOOK_SECRET=
